export enum ButtonVariant {
  PRIMARY = "PRIMARY",
  SECONDARY = "SECONDARY",
  DISABLED = "DISABLED",
  TRANSPARENT = "TRANSPARENT",
}

export enum ButtonType {
  BUTTON = "button",
  SUBMIT = "submit",
  RESET = "reset",
}

export enum ICON_POSITION {
  LEFT = "left",
  RIGHT = "right",
}
