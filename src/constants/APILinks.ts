export const APILinks = {
  getCountryList: () => `https://restcountries.com/v3.1/all`,
  // Add Service API links here
  // login: () => `/auth/login`,
  // getUser: (userId: string) => `/user/single-user/${userId}`,
};

export default APILinks;
